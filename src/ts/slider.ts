import { parse } from "path";


/*

class Model {
	constructor() { }
};

class View {

	app: HTMLElement = document.body;
	range_slider: HTMLDivElement = document.createElement('div');
	trace: HTMLDivElement = document.createElement('div');
	handler: HTMLDivElement = document.createElement('div');
	

	constructor() {
		this.range_slider.className = 'range'
		this.trace.className = 'range__trace'
		this.handler.className = 'range__handler'

		this.app.append(this.range_slider)
		this.range_slider.append(this.trace, this.handler)

	}


};

class Controller {
	model: object;
	view: object;
	constructor(model: object, view: object) {
		this.model = model
		this.view = view
	}
};

const app = new Controller(new Model(), new View());

*/